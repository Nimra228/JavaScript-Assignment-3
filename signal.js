var trafficSignal = prompt("Please enter the traffic signal's color");

if (trafficSignal == "Red") {
    alert("Must Stop!");
}

if (trafficSignal == "Yellow") {
    alert("Ready to move.");
} else if (trafficSignal = "Green") {
    alert("Move Now!");
}